import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("Settings coming soon")
                .navigationTitle("Settings")
        }
    }
}

#Preview {
    SettingsView()
}
